# hsi_mask error messages match snapshot

    Code
      hsi_mask(x = test_reflectance, mask = multi_mask)
    Condition
      Error in `hsi_mask()`:
      ! `mask` must contain exactly 1 layer, not 2.


% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_mask.R
\name{hsi_mask}
\alias{hsi_mask}
\title{Mask hyperspectral raster}
\usage{
hsi_mask(x, mask, inverse = FALSE, filename = "", overwrite = FALSE, ...)
}
\arguments{
\item{x}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with hyperspectral data.}

\item{mask}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with a single mask
layer. Must have the same extent and resolution as \code{x}.}

\item{inverse}{Logical. How \code{mask} is read. Default \code{FALSE}.
\describe{
\item{\code{FALSE}}{Keep-mask: nonzero cells are kept, \code{0} cells dropped.}
\item{\code{TRUE}}{Bad-mask: nonzero cells are dropped, \code{0} cells kept.}
}}

\item{filename}{Character. Output filename. Default \code{""} keeps result in memory.}

\item{overwrite}{Logical. Overwrite existing file. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[terra:writeRaster]{terra::writeRaster()}}.}
}
\value{
A \code{\link[terra:SpatRaster-class]{SpatRaster}} with masked values.
}
\description{
Mask hyperspectral raster
}
\details{
Dropped cells become \code{NA} in every layer of \code{x}. A single mask layer is
recycled across all layers, so each cell is kept or dropped for its whole
spectrum.

Cells that are \code{NA} in \code{mask} are dropped whatever \code{inverse} is set to. \code{NA}
marks a cell the mask carries no information about, and an unknown cell is
discarded rather than trusted. This departs from \code{\link[terra:mask]{terra::mask()}}, which
keeps \code{NA} cells when \code{inverse = TRUE}.

Mask \emph{creation} is out of scope: what counts as background, defect, or
specimen is domain knowledge that varies by material and study. Masks are
built upstream, whether by thresholding a band or index, by rasterising
digitised polygons with \code{\link[terra:rasterize]{terra::rasterize()}}, or by any other rule.

Combine several sources in mask space rather than calling this function
repeatedly. Masks are single-layer and cheap to combine, while \code{x} is the
expensive side. Keep every source on one polarity while combining: bad-masks
combine as a union (\code{any()}), keep-masks as an intersection (\code{all()}), and
mixing the two silently turns one into the other. Note that
\code{\link[terra:rasterize]{terra::rasterize()}} writes \code{NA}, not \code{0}, outside its polygons, so pass
\code{background = 0} to keep the result usable in that algebra.

Masking precedes endmember search and pixel statistics. Background and tray
pixels are spectrally distinct from the specimen, and will be selected as
endmembers or skew band statistics if left in.
}
\examples{
\dontrun{
x <- terra::rast("REFLECTANCE_testdata.tif")

# Keep-mask: nonzero marks the specimen to keep
specimen <- terra::rast("SPECIMEN_testdata.tif")

x_mask <- hsi_mask(x, specimen)

# Bad-mask: nonzero marks defects to drop
defects <- terra::rast("DEFECTS_testdata.tif")

x_mask <- hsi_mask(x, defects, inverse = TRUE)

x_mask <- hsi_mask(
  x,
  defects,
  inverse = TRUE,
  filename = "REFLECTANCE_mask.tif",
  overwrite = TRUE
)
}

}
\seealso{
\code{\link[=hsi_destripe]{hsi_destripe()}}, which likewise requires background pixels excluded.

Other HSI Transformations:
\code{\link[=hsi_apply_mnf]{hsi_apply_mnf()}},
\code{\link[=hsi_calc_difference]{hsi_calc_difference()}},
\code{\link[=hsi_calc_mnf]{hsi_calc_mnf()}},
\code{\link[=hsi_calc_ndi]{hsi_calc_ndi()}},
\code{\link[=hsi_calc_raba]{hsi_calc_raba()}},
\code{\link[=hsi_calc_rabd]{hsi_calc_rabd()}},
\code{\link[=hsi_calc_ratio]{hsi_calc_ratio()}},
\code{\link[=hsi_calc_rcv]{hsi_calc_rcv()}},
\code{\link[=hsi_calc_reflectance]{hsi_calc_reflectance()}},
\code{\link[=hsi_calc_remp]{hsi_calc_remp()}},
\code{\link[=hsi_calc_rmean]{hsi_calc_rmean()}},
\code{\link[=hsi_calc_rmedian]{hsi_calc_rmedian()}},
\code{\link[=hsi_calc_rsd]{hsi_calc_rsd()}},
\code{\link[=hsi_calc_stretch]{hsi_calc_stretch()}},
\code{\link[=hsi_destripe]{hsi_destripe()}},
\code{\link[=hsi_remove_continuum]{hsi_remove_continuum()}},
\code{\link[=hsi_smooth_median]{hsi_smooth_median()}},
\code{\link[=hsi_smooth_savgol]{hsi_smooth_savgol()}},
\code{\link[=hsi_tiled]{hsi_tiled()}},
\code{\link[=hsi_write_scaled]{hsi_write_scaled()}}
}
\concept{HSI Transformations}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_find_extent.R, R/utils.R
\name{hsi_find_extent}
\alias{hsi_find_extent}
\title{Find a fixed-width extent from reference points}
\usage{
hsi_find_extent(e, width)

hsi_find_extent(e, width)
}
\arguments{
\item{e}{A \code{\link[terra:SpatExtent-class]{SpatExtent}} in which to search.
Created with \code{\link[terra:ext]{terra::ext()}}.}

\item{width}{Numeric. Width in pixels of the new extent.}

\item{x}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with hyperspectral data.}

\item{points}{A \code{\link[terra:SpatVector-class]{SpatVector}} with exactly 2 point
geometries marking the vertical extent of the region of interest. Must be
in the same coordinate space as \code{x}.}

\item{filename}{Character. Output filename. Default \code{""} skips writing.}

\item{overwrite}{Logical. Overwrite existing file. Default \code{FALSE}.}

\item{insert}{Logical. Insert layer into an existing file. Default \code{FALSE}.}

\item{layer}{Character. Layer name for vector output. Default \code{""}.}

\item{...}{Additional arguments passed to \code{\link[terra:writeVector]{terra::writeVector()}}.}
}
\value{
A \code{\link[terra:SpatVector-class]{SpatVector}} polygon snapped to the
grid of \code{x}.

A \code{\link[terra:SpatExtent-class]{SpatExtent}} centered within \code{extent}
and of the specified width.
}
\description{
Find a fixed-width extent from reference points

Find a fixed-width extent
}
\details{
The output polygon spans vertically between the two reference points and
horizontally by \code{width} pixels centered on the mean x-coordinate of the
points. The extent is snapped to cell boundaries of \code{x} with
\code{\link[terra:align]{terra::align()}} using \code{snap = "near"}.

The function aborts if the requested extent falls outside the raster bounds.
Reduce \code{width} or adjust the reference points to fit within \code{x}.
}
\examples{
\dontrun{
x <- terra::rast("REFLECTANCE_testdata.tif")
ends <- terra::vect("spatials.gpkg", layer = "ends")

x_extent <- hsi_find_extent(x, points = ends, width = 900)
x_cropped <- terra::crop(x, x_extent)

x_extent <- hsi_find_extent(
  x,
  points = ends,
  width = 900,
  filename = "spatials.gpkg",
  insert = TRUE,
  layer = "extent"
)
}

}
\seealso{
\code{\link[=hsi_set_physical_extent]{hsi_set_physical_extent()}} to assign physical units to a raster extent.

Other Utilities: 
\code{\link{hsi_bind_rows}()},
\code{\link{hsi_merge_rasters}()},
\code{\link{hsi_subset}()},
\code{\link{hsi_subset_range}()},
\code{\link{wavelength_position}()}

Other Utilities: 
\code{\link{hsi_bind_rows}()},
\code{\link{hsi_merge_rasters}()},
\code{\link{hsi_subset}()},
\code{\link{hsi_subset_range}()},
\code{\link{wavelength_position}()}
}
\concept{Utilities}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_plot_profile.R
\name{hsi_plot_profile}
\alias{hsi_plot_profile}
\title{Plot a depth profile}
\usage{
hsi_plot_profile(x)
}
\arguments{
\item{x}{A \link[tibble:tibble]{tibble} with columns \code{position} and exactly one
value column, as produced by \code{\link[=hsi_extract_profile]{hsi_extract_profile()}}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot}} object. Extend with \code{+} to add labels,
themes, or colour scales.
}
\description{
Plot a depth profile
}
\details{
Produces a minimal stratigraphic profile plot. Position is mapped to the
x-axis as the independent variable — ensuring that stats like
\code{\link[ggplot2:geom_smooth]{ggplot2::geom_smooth()}} work correctly — then \code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}}
rotates the plot so that depth runs top-to-bottom visually.
\code{\link[ggplot2:scale_x_reverse]{ggplot2::scale_x_reverse()}} places shallow positions at the top.

When profile unit metadata is present, tick labels include the unit suffix
such as \verb{0 mm} or \verb{1.5 cm}. If no unit metadata exists, ggplot2 default
labels are used, showing pixel coordinates or raw position values.

The returned ggplot carries no theme or axis labels — add these with \code{+}
using standard ggplot2 conventions.
}
\examples{
\dontrun{
x <- terra::rast("RABD_testdata.tif") |> terra::subset(1)
x_profile <- hsi_extract_profile(x)

# Quick pixel-space profile
x_plot <- hsi_plot_profile(x_profile)

# Physical-space profile
um <- hsi_calibration_from_dims(scan_length_um = 50000, n_pixels = 1000)
ref <- terra::vect(matrix(c(1001.5, 2007.5), ncol = 2), type = "points")
x_cal <- hsi_calibrate_raster(x, reference = ref, um_per_pixel = um)
x_profile <- hsi_extract_profile(x_cal)
x_plot <- hsi_plot_profile(x_profile)

# Add labels and theme with ggplot2
x_plot +
  ggplot2::labs(x = "Depth (cm)", y = "RABD") +
  ggplot2::theme_minimal()
}

}
\seealso{
\code{\link[=hsi_extract_profile]{hsi_extract_profile()}} to produce the input tibble.
\code{\link[=hsi_plot_spectrum]{hsi_plot_spectrum()}} for spectral plots.
\code{\link[=hsi_plot_raster]{hsi_plot_raster()}} for spatial raster maps.

Other Plotting:
\code{\link[=hsi_plot_composite]{hsi_plot_composite()}},
\code{\link[=hsi_plot_raster]{hsi_plot_raster()}},
\code{\link[=hsi_plot_raster_rgb]{hsi_plot_raster_rgb()}},
\code{\link[=hsi_plot_spectrum]{hsi_plot_spectrum()}}
}
\concept{Plotting}

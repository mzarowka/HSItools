% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_calc_mnf.R
\name{hsi_calc_mnf}
\alias{hsi_calc_mnf}
\title{Compute Minimum Noise Fraction transform}
\usage{
hsi_calc_mnf(x, trim = 0L, ...)
}
\arguments{
\item{x}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with hyperspectral data.}

\item{trim}{Non-negative integer. Number of bands to drop symmetrically from
both spectral edges before computing the transform. Default \code{0L} applies
no trimming.}

\item{...}{Additional arguments passed to \code{\link[spacetime:mnf]{spacetime::mnf()}}, notably
\code{Sigma.Noise} for a custom noise covariance matrix and \code{use} for NA
handling. Default noise estimate uses \code{0.5 * cov(diff(x))} (MAF).}
}
\value{
An object of class \code{c("mnf", "prcomp")} with components:
\item{values}{Numeric vector of eigenvalues (noise fractions), one per band.}
\item{rotation}{Numeric matrix of eigenvectors (loadings).}
\item{x}{Numeric matrix of MNF scores (pixels x bands).}
}
\description{
Compute Minimum Noise Fraction transform
}
\details{
MNF components are ordered by \strong{decreasing noise fraction}: MNF1 carries
the most noise, the final components carry the most signal. Eigenvalues
approximate the noise fraction under the proportional covariance model and
are non-negative, but may exceed 1 with real data. \code{1 - eigenvalue} is the
lag-1 autocorrelation of that component. Inspect \verb{$values} to identify the
signal-rich tail before passing the result to \code{hsi_apply_mnf()}.

When \code{x} has been pre-processed with \code{\link[=hsi_smooth_savgol]{hsi_smooth_savgol()}}, spectral edge
bands become nearly collinear due to the polynomial fitting, which can make
the noise covariance matrix singular. Set \code{trim} to \code{ceiling(n / 2)} where
\code{n} is the window size passed to \code{\link[=hsi_smooth_savgol]{hsi_smooth_savgol()}} to drop the
affected bands from both ends before computing the transform.

Wraps \code{\link[spacetime:mnf]{spacetime::mnf()}} by Edzer Pebesma, implementing the algorithm of
Green et al. (1988) with noise estimation following Switzer & Green (1984).

Green, A.A., Berman, M., Switzer, P. and Craig, M.D. (1988). A
transformation for ordering multispectral data in terms of image quality
with implications for noise removal. \emph{IEEE Transactions on Geoscience and
Remote Sensing}, 26(1), 65–74.

Switzer, P. and Green, A. (1984). Min/max autocorrelation factors for
multivariate spatial imagery. Technical Report, Department of Statistics,
Stanford University.
}
\examples{
\dontrun{
x <- terra::rast("REFLECTANCE_testdata.tif")
x_mnf <- hsi_calc_mnf(x)
x_mnf$values

# After hsi_smooth_savgol() with window = 15
x_mnf <- hsi_calc_mnf(x, trim = 8L)
}

}
\seealso{
\code{\link[=hsi_apply_mnf]{hsi_apply_mnf()}}, \code{\link[spacetime:mnf]{spacetime::mnf()}}

Other HSI Transformations: 
\code{\link{hsi_calc_difference}()},
\code{\link{hsi_calc_ndi}()},
\code{\link{hsi_calc_raba}()},
\code{\link{hsi_calc_rabd}()},
\code{\link{hsi_calc_ratio}()},
\code{\link{hsi_calc_rcv}()},
\code{\link{hsi_calc_reflectance}()},
\code{\link{hsi_calc_remp}()},
\code{\link{hsi_calc_rmean}()},
\code{\link{hsi_calc_rmedian}()},
\code{\link{hsi_calc_rsd}()},
\code{\link{hsi_calc_stretch}()},
\code{\link{hsi_destripe}()},
\code{\link{hsi_remove_continuum}()},
\code{\link{hsi_smooth_median}()},
\code{\link{hsi_smooth_savgol}()},
\code{\link{hsi_tiled}()},
\code{\link{hsi_write_scaled}()}
}
\concept{HSI Transformations}

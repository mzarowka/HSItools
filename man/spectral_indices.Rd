% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{spectral_indices}
\alias{spectral_indices}
\title{Preset spectral indices definitions}
\format{
\subsection{\code{spectral_indices}}{

A tibble with 25 rows and 9 columns:
\describe{
\item{proxy_name}{Character. Unique proxy identifier.}
\item{proxy_type}{Character. One of \code{"rabd"}, \code{"raba"}, \code{"ratio"}, \code{"difference"}, \code{"remp"}.}
\item{continuum_edges}{List. Numeric vector of length 2, continuum anchor wavelengths in nm. \code{NA} where not applicable.}
\item{absorption_band}{List. Numeric vector, trough wavelength(s) in nm. Single value for \code{"strict"}, length-2 range for \code{"max"} and \code{"mid"}. \code{NA} where not applicable.}
\item{index_type}{Character. Calculation variant passed to the corresponding function. \code{NA} where not applicable.}
\item{bands}{List. Numeric vector of length 2, band wavelengths in nm. \code{NA} where not applicable.}
\item{search_range}{List. Numeric vector of length 2, search window in nm. \code{NA} defers to function default. \code{NA} where not applicable.}
\item{interpretation}{Character. Typical sedimentological or biogeochemical interpretation.}
\item{reference}{Character. Bibliographic reference. \code{NA} where not sourced from a specific publication.}
}
}
}
\source{
Selected from available literature. See the \code{reference} column for
per-entry citations.
}
\usage{
spectral_indices
}
\description{
Preset spectral indices definitions
}
\details{
Each row maps directly to a single function call. Columns correspond to
argument names in the relevant \verb{hsi_calc_*} function, so a row can be
passed to a function without renaming. Columns that are not used by a given
\code{proxy_type} carry \code{NA}.

Entries marked with \code{index_type = "x"} (RABA rows) use a placeholder
pending finalisation of the RABA calculation variants.

The SWIR entry \code{rabd16601690} carries \code{NA} for all spectral parameters
pending verification.
}
\keyword{datasets}

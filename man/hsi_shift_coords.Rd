% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_shift_coords.R
\name{hsi_shift_coords}
\alias{hsi_shift_coords}
\title{Shift physical coordinate raster to a known reference position}
\usage{
hsi_shift_coords(
  x,
  reference,
  origin = 0,
  filename = "",
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with two layers
\code{row_um} and \code{col_um}, as produced by \code{\link[=hsi_calc_coords]{hsi_calc_coords()}}.}

\item{reference}{A \code{\link[terra:SpatVector-class]{SpatVector}} with a single
point geometry in the same pixel coordinate space as \code{x}.}

\item{origin}{Numeric. Known physical position of the reference point
in µm. Default \code{0} places the reference point at zero, with positions
above returning negative values.}

\item{filename}{Character. Output filename. Default \code{""} keeps result in memory.}

\item{overwrite}{Logical. Overwrite existing file. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[terra:writeRaster]{terra::writeRaster()}}.}
}
\value{
A \code{\link[terra:SpatRaster-class]{SpatRaster}} with two layers: \code{row_um}
(shifted physical row position in µm) and \code{col_um} (unchanged physical
column position in µm).
}
\description{
Shift physical coordinate raster to a known reference position
}
\details{
The shift is computed by extracting the \code{row_um} value at \code{reference} and
subtracting \code{origin} from it. The resulting offset is applied to the entire
\code{row_um} layer. Pixels above the reference point will hold negative values
when \code{origin = 0}.

\code{reference} must share the same pixel coordinate space as \code{x} — no CRS is
expected on either input. The point is typically digitised from the
coordinate raster directly, for example in QGIS or via \code{\link[terra:click]{terra::click()}}.
}
\examples{
\dontrun{
x <- terra::rast("REFLECTANCE_testdata.tif")
um <- hsi_calibration_from_dims(scan_length_um = 50000, n_pixels = 1000)
x_coords <- hsi_calc_coords(x, um_per_pixel = um)

ref <- terra::vect(matrix(c(1001.5, 2007.5), ncol = 2), type = "points")

x_coords_shifted <- hsi_shift_coords(x_coords, reference = ref)
x_coords_shifted <- hsi_shift_coords(
  x_coords,
  reference = ref,
  origin = 300000,
  filename = "coords_shifted.tif",
  overwrite = TRUE
)
}

}
\seealso{
\code{\link[=hsi_calc_coords]{hsi_calc_coords()}} to produce the input coordinate raster.

Other HSI Calibration: 
\code{\link{hsi_calc_coords}()},
\code{\link{hsi_calibration_direct}()},
\code{\link{hsi_calibration_from_dims}()},
\code{\link{hsi_calibration_from_scale}()},
\code{\link{hsi_drop_crs}()},
\code{\link{hsi_pixels_to_units}()},
\code{\link{hsi_set_physical_extent}()}
}
\concept{HSI Calibration}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_set_physical_extent.R
\name{hsi_set_physical_extent}
\alias{hsi_set_physical_extent}
\title{Assign a physically calibrated spatial extent to a SpatRaster}
\usage{
hsi_set_physical_extent(
  x,
  y = NULL,
  units = "mm",
  filename = "",
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with hyperspectral data.}

\item{y}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with physical coordinate
layers \code{row_um} and \code{col_um}, as produced by \code{\link[=hsi_calc_coords]{hsi_calc_coords()}} or
\code{\link[=hsi_shift_coords]{hsi_shift_coords()}}. Must have the same number of rows and columns as
\code{x}. Default \code{NULL} returns \code{x} unchanged.}

\item{units}{Character. Physical units for the output extent. One of
\code{"um"}, \code{"mm"}, or \code{"cm"}. Default \code{"mm"}.}

\item{filename}{Character. Output filename. Default \code{""} keeps result in memory.}

\item{overwrite}{Logical. Overwrite existing file. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[terra:writeRaster]{terra::writeRaster()}}.}
}
\value{
A \code{\link[terra:SpatRaster-class]{SpatRaster}} with hyperspectral data
and a physically calibrated spatial extent.
}
\description{
Assign a physically calibrated spatial extent to a SpatRaster
}
\details{
Replaces the spatial extent of a deep copy of \code{x} with a physically
calibrated extent derived from \code{y}. The result can be cropped in physical
units directly with \code{\link[terra:crop]{terra::crop()}}, or passed to
\code{\link[=hsi_plot_spatraster]{hsi_plot_spatraster()}} or \code{\link[=hsi_plot_spatraster_rgb]{hsi_plot_spatraster_rgb()}} without
supplying \code{y} — the extent is already embedded in the raster.

Terra uses a y-up convention: \code{ymax} corresponds to row 1 and \code{ymin} to
the last row. Because depth increases downward in sediment core scans, the
row coordinate is negated when building the extent — \code{ymin} receives
\code{-max(row_um)} and \code{ymax} receives \code{-min(row_um)}. This satisfies terra's
requirement that \code{ymin < ymax} while placing shallow positions at the top
of any downstream plot. When \code{y} contains negative \code{row_um} values (i.e.
the origin was shifted with \code{\link[=hsi_shift_coords]{hsi_shift_coords()}}), the negation still
holds correctly — positions above the reference point appear as positive
\code{ymax} values.

\code{\link[terra:deepcopy]{terra::deepcopy()}} is used to ensure the caller's raster is never
mutated, since terra's C++ backend can share object state across R names.
}
\examples{
\dontrun{
x <- terra::rast("REFLECTANCE_testdata.tif")
um <- hsi_calibration_from_dims(scan_length_um = 50000, n_pixels = 1000)
x_coords <- hsi_calc_coords(x, um_per_pixel = um)

x_physical <- hsi_set_physical_extent(x, y = x_coords)
x_physical <- hsi_set_physical_extent(
  x,
  y = x_coords,
  units = "mm",
  filename = "x_physical.tif",
  overwrite = TRUE
)
}

}
\seealso{
\code{\link[=hsi_calc_coords]{hsi_calc_coords()}} and \code{\link[=hsi_shift_coords]{hsi_shift_coords()}} to produce \code{y}.
\code{\link[=hsi_plot_spatraster]{hsi_plot_spatraster()}} and \code{\link[=hsi_plot_spatraster_rgb]{hsi_plot_spatraster_rgb()}} for
downstream plotting.

Other HSI Calibration: 
\code{\link{hsi_calc_coords}()},
\code{\link{hsi_calibration_direct}()},
\code{\link{hsi_calibration_from_dims}()},
\code{\link{hsi_calibration_from_scale}()},
\code{\link{hsi_drop_crs}()},
\code{\link{hsi_pixels_to_units}()},
\code{\link{hsi_shift_coords}()}
}
\concept{HSI Calibration}

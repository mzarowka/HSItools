% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_tiled.R
\name{hsi_tiled}
\alias{hsi_tiled}
\title{Process a SpatRaster in parallel tiles}
\usage{
hsi_tiled(fun, x, n_tiles, filename = "", overwrite = FALSE, ...)
}
\arguments{
\item{fun}{Function. Applied to each tile. Must be written as an anonymous
function with explicit \verb{HSItools::} namespacing, e.g.
\verb{\\(tile) HSItools::hsi_smooth_savgol(tile, p = 3, n = 17)}. Only suitable
for per-pixel operations with no spatial neighbourhood dependency. All
parameters must be supplied as literal values — variables from the calling
environment are not visible to parallel workers.}

\item{x}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with hyperspectral data.}

\item{n_tiles}{Integer or integer vector of length 1 or 2. Number of tiles
to split \code{x} into. A single integer creates row strips (e.g. \code{30}). A
length-2 vector creates a 2D tile grid (e.g. \code{c(8, 8)}). For best
performance, match to the number of available \code{mirai} daemons.}

\item{filename}{Character. Output filename. Default \code{""} writes the result
to a session-scoped temporary file and emits a warning. Providing a path
is strongly recommended. Unlike other \code{HSItools} functions, \code{filename = ""}
never keeps the result in memory — see Details.}

\item{overwrite}{Logical. Overwrite existing file. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[terra:writeRaster]{terra::writeRaster()}}.}
}
\value{
A \code{\link[terra:SpatRaster-class]{SpatRaster}} merged from processed tiles.
}
\description{
Process a SpatRaster in parallel tiles
}
\details{
Parallelism is provided by \code{\link[mirai:mirai_map]{mirai::mirai_map()}} and \code{\link[mirai:daemons]{mirai::daemons()}}.
Daemons must be initialised by the caller before invoking this function via
\code{mirai::daemons(n)}. If no daemons are active, the function errors.

\strong{This function does not support in-memory processing.} \code{\link[terra:makeTiles]{terra::makeTiles()}}
requires a filename and errors if one is not provided — tiles are always
written to disk. As a consequence, the \code{in_memory} parameter present in
other \code{HSItools} functions is intentionally absent here. The merged result
is always file-backed: either the path supplied via \code{filename}, or a
session-scoped temporary file when \code{filename = ""}. In the latter case a
warning is emitted and the temporary file persists until the R session ends.

Intermediate tiles are written to a session-scoped temporary directory that
is not cleaned up on function exit. This is intentional: persistent \code{mirai}
daemon processes may hold open GDAL file handles to tile files after the
function returns, and premature cleanup causes access violations on Windows.
}
\examples{
\dontrun{
mirai::daemons(30)

# Recommended: always provide a filename
hsi_tiled(
  fun = \(tile) HSItools::hsi_smooth_savgol(tile, p = 3, n = 17),
  x = my_raster,
  n_tiles = 30,
  filename = "output.tif",
  overwrite = TRUE
)

# Bad: variables from calling environment are not visible to workers
p <- 3
n <- 17
hsi_tiled(
  fun = \(tile) HSItools::hsi_smooth_savgol(tile, p = p, n = n),
  x = my_raster,
  n_tiles = 30
)

mirai::daemons(0)
}

}
\seealso{
Other HSI Transformations:
\code{\link[=hsi_apply_mnf]{hsi_apply_mnf()}},
\code{\link[=hsi_calc_difference]{hsi_calc_difference()}},
\code{\link[=hsi_calc_mnf]{hsi_calc_mnf()}},
\code{\link[=hsi_calc_ndi]{hsi_calc_ndi()}},
\code{\link[=hsi_calc_preview]{hsi_calc_preview()}},
\code{\link[=hsi_calc_raba]{hsi_calc_raba()}},
\code{\link[=hsi_calc_rabd]{hsi_calc_rabd()}},
\code{\link[=hsi_calc_ratio]{hsi_calc_ratio()}},
\code{\link[=hsi_calc_rcv]{hsi_calc_rcv()}},
\code{\link[=hsi_calc_reflectance]{hsi_calc_reflectance()}},
\code{\link[=hsi_calc_remp]{hsi_calc_remp()}},
\code{\link[=hsi_calc_rmean]{hsi_calc_rmean()}},
\code{\link[=hsi_calc_rmedian]{hsi_calc_rmedian()}},
\code{\link[=hsi_calc_rsd]{hsi_calc_rsd()}},
\code{\link[=hsi_calc_stretch]{hsi_calc_stretch()}},
\code{\link[=hsi_destripe]{hsi_destripe()}},
\code{\link[=hsi_mask]{hsi_mask()}},
\code{\link[=hsi_remove_continuum]{hsi_remove_continuum()}},
\code{\link[=hsi_smooth_median]{hsi_smooth_median()}},
\code{\link[=hsi_smooth_savgol]{hsi_smooth_savgol()}},
\code{\link[=hsi_write_scaled]{hsi_write_scaled()}}
}
\concept{HSI Transformations}

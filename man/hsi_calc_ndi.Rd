% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_calc_ndi.R
\name{hsi_calc_ndi}
\alias{hsi_calc_ndi}
\title{Calculate normalized difference index (NDI)}
\usage{
hsi_calc_ndi(
  x,
  bands,
  index_name = NULL,
  filename = "",
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{x}{A terra SpatRaster with hyperspectral data}

\item{bands}{Numeric vector of length 2. The two wavelengths (in nm) to use
for NDI calculation}

\item{index_name}{Character. Name of calculated ratio. Default NULL}

\item{filename}{Character. Output filename. Default "" keeps in memory}

\item{overwrite}{Logical. Overwrite existing file (default: FALSE)}

\item{...}{Additional arguments passed to \code{\link[terra]{writeRaster}}}
}
\value{
A terra SpatRaster with ndi values
}
\description{
Calculate a Normalized Difference Index (NDI), which normalizes the difference
between two bands by their sum. This approach is widely used in remote sensing
(e.g., NDVI, NDWI) as it reduces the effects of illumination and viewing
geometry while highlighting spectral contrasts.

calculate normalized difference index
}
\examples{
\dontrun{
# Load hyperspectral data
x <- terra::rast("REFLECTANCE_testdata.tif")

# Calculate band ratio between 570 and 690
x_ndi <- hsi_calc_ndi(
 x,
 bands = c(570, 690)
)

# Save to file and provide a name
x_ndi <- hsi_calc_ndi(
 x,
 bands = c(570, 690)
 index_name = "ndi570690",
 filename = "output_ndi.tif",
 overwrite = TRUE
)
}

}
\seealso{
Other HSI Transformations: 
\code{\link{hsi_calc_derivative}()},
\code{\link{hsi_calc_difference}()},
\code{\link{hsi_calc_raba}()},
\code{\link{hsi_calc_rabd}()},
\code{\link{hsi_calc_ratio}()},
\code{\link{hsi_calc_rcv}()},
\code{\link{hsi_calc_remp}()},
\code{\link{hsi_calc_rmean}()},
\code{\link{hsi_calc_rmedian}()},
\code{\link{hsi_calc_rsd}()},
\code{\link{hsi_calc_stretch}()},
\code{\link{hsi_normalize}()},
\code{\link{hsi_remove_continuum}()},
\code{\link{hsi_smooth_median}()},
\code{\link{hsi_smooth_savgol}()}
}
\concept{HSI Transformations}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_classifications.R
\name{find_optimal_clusters}
\alias{find_optimal_clusters}
\title{Find optimal number of clusters for k-means clustering of hyperspectral data}
\usage{
find_optimal_clusters(
  raster,
  max_clusters = 10,
  min_clusters = 2,
  methods = c("elbow", "silhouette", "ch_index", "db_index"),
  sample_size = 10000,
  nstart = 25,
  iter.max = 100,
  plot = TRUE,
  extent = NULL,
  ext = "png",
  filename = NULL
)
}
\arguments{
\item{raster}{terra SpatRaster to analyze.}

\item{max_clusters}{maximum number of clusters to evaluate.}

\item{min_clusters}{minimum number of clusters to evaluate (default is 2).}

\item{methods}{vector of methods to use: "elbow", "silhouette", "gap", "ch_index", "db_index".}

\item{sample_size}{number of sample pixels to use (default is 10000).}

\item{nstart}{number of random starts for k-means (default is 25).}

\item{iter.max}{maximum iterations for k-means (default is 100).}

\item{plot}{logical, whether to create plots (default is TRUE).}

\item{extent}{an extent or SpatVector used to subset SpatRaster. Defaults to the entire SpatRaster.}

\item{ext}{character, a graphic format extension for saving plots.}

\item{filename}{NULL (default) for no file output, or specify to save plots.}
}
\value{
A list containing the optimal number of clusters according to different methods,
metrics for each evaluated number of clusters, and ggplot objects.
}
\description{
Determines the optimal number of clusters for k-means clustering using multiple methods:
\enumerate{
\item Elbow method (Within-cluster Sum of Squares)
\item Silhouette method (Average silhouette width)
\item Gap statistic (Comparison to null reference)
\item Calinski-Harabasz index (Variance ratio criterion)
\item Davies-Bouldin index (Average similarity between clusters)
}
}
\seealso{
Other Spectral classifications: 
\code{\link{calculate_kmeans}()},
\code{\link{calculate_kmeans_multiple}()},
\code{\link{extract_kendmembers}()}
}
\concept{Spectral classifications}

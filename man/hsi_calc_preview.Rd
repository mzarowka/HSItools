% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_calc_preview.R
\name{hsi_calc_preview}
\alias{hsi_calc_preview}
\title{Calibrate a three-band preview composite}
\usage{
hsi_calc_preview(
  x,
  whiteref,
  darkref,
  darkspec = NULL,
  tint = c(1, 1),
  type,
  tol = 25,
  filename = "",
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with raw
hyperspectral sample data. Band names must be numeric wavelengths in nm.}

\item{whiteref}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with white
reference data. Must have the same bands and wavelengths as \code{x}.}

\item{darkref}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with dark
reference data from the white reference session. Must have the same bands
and wavelengths as \code{x}.}

\item{darkspec}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with dark
reference data from the specimen session. Default \code{NULL}. Required for
dual-exposure workflows where \code{tint} values differ. When \code{NULL} and
\code{tint = c(1, 1)}, a single dark reference is sufficient.}

\item{tint}{Numeric vector of length 2. Integration times for white
reference and specimen capture, in that order. Default \code{c(1, 1)} assumes
equal integration times.}

\item{type}{Character or numeric. A predefined band combination
(\code{"RGB"}, \code{"CIR"}, \code{"NIR"}, \code{"SWIR"}) or a numeric vector of exactly 3
wavelengths in nm.}

\item{tol}{Numeric. Wavelength tolerance for band matching in nm. Default \code{25}.}

\item{filename}{Character. Output filename. Default \code{""} keeps result in memory.}

\item{overwrite}{Logical. Overwrite existing file. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[terra:writeRaster]{terra::writeRaster()}}.}
}
\value{
A \code{\link[terra:SpatRaster-class]{SpatRaster}} with 3 reflectance bands,
named by the wavelengths of the matched bands.
}
\description{
Calibrate only the three bands needed for a false-colour composite, instead
of converting an entire cube to reflectance and discarding all but three
bands afterwards. Intended for rapid visual inspection and markup.
}
\details{
The requested wavelengths are resolved against the band grid of \code{x} once,
and the resulting band indices are applied to \code{x}, \code{whiteref}, \code{darkref},
and \code{darkspec} alike. Resolving each raster independently would allow
rounding differences between band labels to pull the specimen and its
references onto different bands, silently calibrating one wavelength
against another. All inputs must therefore share the same number of bands.

Calibration is delegated to \code{\link[=hsi_calc_reflectance]{hsi_calc_reflectance()}} and follows the same
three paths, selected by \code{darkspec} and \code{tint}; see its documentation for
the formulas. Processing is forced in memory, since three bands are small
by construction.

Bands are matched to the nearest available wavelength, but a match further
than \code{tol} from the request is an error rather than a silent substitution —
without it, asking for a \code{"SWIR"} composite of a VNIR capture would return
the nearest edge band three times over. The result is a reflectance raster,
not a stretched one; pipe it through \code{\link[=hsi_calc_stretch]{hsi_calc_stretch()}} for display.
}
\examples{
\dontrun{
x <- terra::rast("capture/testdata.tif")
whiteref <- terra::rast("capture/WHITEREF_testdata.tif")
darkref <- terra::rast("capture/DARKREF_testdata.tif")

x_preview <- hsi_calc_preview(
  x = x,
  whiteref = whiteref,
  darkref = darkref,
  type = "RGB"
)

# Ready to display
x_preview |>
  hsi_calc_stretch(type = "RGB") |>
  hsi_plot_raster_rgb()

# Custom wavelengths, matched darks, written to disk
darkspec <- terra::rast("specimen/DARKREF_testdata.tif")

x_preview <- hsi_calc_preview(
  x = x,
  whiteref = whiteref,
  darkref = darkref,
  darkspec = darkspec,
  tint = c(3, 9),
  type = c(700, 620, 540),
  filename = "output_preview.tif",
  overwrite = TRUE
)
}

}
\seealso{
\code{\link[=hsi_calc_reflectance]{hsi_calc_reflectance()}} for calibrating a full cube.
\code{\link[=hsi_calc_stretch]{hsi_calc_stretch()}} for stretching the result to a displayable range.
\code{\link[=hsi_plot_raster_rgb]{hsi_plot_raster_rgb()}} for rendering the composite.

Other HSI Transformations:
\code{\link[=hsi_apply_mnf]{hsi_apply_mnf()}},
\code{\link[=hsi_calc_difference]{hsi_calc_difference()}},
\code{\link[=hsi_calc_mnf]{hsi_calc_mnf()}},
\code{\link[=hsi_calc_ndi]{hsi_calc_ndi()}},
\code{\link[=hsi_calc_raba]{hsi_calc_raba()}},
\code{\link[=hsi_calc_rabd]{hsi_calc_rabd()}},
\code{\link[=hsi_calc_ratio]{hsi_calc_ratio()}},
\code{\link[=hsi_calc_rcv]{hsi_calc_rcv()}},
\code{\link[=hsi_calc_reflectance]{hsi_calc_reflectance()}},
\code{\link[=hsi_calc_remp]{hsi_calc_remp()}},
\code{\link[=hsi_calc_rmean]{hsi_calc_rmean()}},
\code{\link[=hsi_calc_rmedian]{hsi_calc_rmedian()}},
\code{\link[=hsi_calc_rsd]{hsi_calc_rsd()}},
\code{\link[=hsi_calc_stretch]{hsi_calc_stretch()}},
\code{\link[=hsi_destripe]{hsi_destripe()}},
\code{\link[=hsi_mask]{hsi_mask()}},
\code{\link[=hsi_remove_continuum]{hsi_remove_continuum()}},
\code{\link[=hsi_smooth_median]{hsi_smooth_median()}},
\code{\link[=hsi_smooth_savgol]{hsi_smooth_savgol()}},
\code{\link[=hsi_tiled]{hsi_tiled()}},
\code{\link[=hsi_write_scaled]{hsi_write_scaled()}}
}
\concept{HSI Transformations}

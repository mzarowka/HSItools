% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_bind_rows.R
\name{hsi_bind_rows}
\alias{hsi_bind_rows}
\title{Bind hyperspectral SpatRasters along the y-axis}
\usage{
hsi_bind_rows(x, filename = "", overwrite = FALSE, ...)
}
\arguments{
\item{x}{List of \code{\link[terra:SpatRaster-class]{SpatRaster}} objects with
hyperspectral data, in the desired top-to-bottom order. All elements must
share the same number of layers, layer names, and spatial resolution.}

\item{filename}{Character. Output filename. Default \code{""} keeps result in memory.}

\item{overwrite}{Logical. Overwrite existing file. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[terra:merge]{terra::merge()}}.}
}
\value{
A \code{\link[terra:SpatRaster-class]{SpatRaster}} with all inputs bound
along the y-axis in the order provided.
}
\description{
Bind hyperspectral SpatRasters along the y-axis
}
\details{
Concatenates SpatRasters along the y-axis in the order provided. All
rasters are left-aligned to \code{xmin = 0}. Rasters narrower than the widest
input are padded with \code{NA} columns on the right via \code{\link[terra:extend]{terra::extend()}},
so the output forms a rectangular block rather than a Tetris-like shape.

Resolution, layer count, and layer names (wavelengths) must be identical
across all inputs — the function aborts if any mismatch is detected.
}
\examples{
\dontrun{
sections <- list(section_01, section_02, section_03, section_04)
x_bound <- hsi_bind_rows(sections, filename = "bound.tif", overwrite = TRUE)
}

}
\seealso{
\code{\link[=hsi_bind_layers]{hsi_bind_layers()}} for concatenation along the spectral (layer) axis.

Other Utilities:
\code{\link[=hsi_bind_layers]{hsi_bind_layers()}},
\code{\link[=hsi_find_extent]{hsi_find_extent()}},
\code{\link[=hsi_subset]{hsi_subset()}},
\code{\link[=hsi_subset_range]{hsi_subset_range()}},
\code{\link[=wavelength_position]{wavelength_position()}}
}
\concept{Utilities}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_calibration_from_scale.R
\name{hsi_calibration_from_scale}
\alias{hsi_calibration_from_scale}
\title{Create spatial calibration from a digitized scale reference}
\usage{
hsi_calibration_from_scale(x, distance = 10000, units = "um")
}
\arguments{
\item{x}{A \code{\link[terra:SpatVector-class]{SpatVector}} representing a digitized
scale reference. Must be either a line geometry or exactly 2 points.
CRS must be \code{NULL} (pixel coordinates).}

\item{distance}{Numeric. Physical distance the reference represents.
Single positive value. Default \code{10000}.}

\item{units}{Character. Units of the provided distance. One of \code{"um"}
(micrometers), \code{"mm"}, or \code{"cm"}. Default \code{"um"}.}
}
\value{
A named numeric with resolution in µm/px, named \code{"um_per_px"}.
}
\description{
Create a spatial calibration by measuring a known-length reference
digitized in GIS software. The function calculates pixel distance from
the geometry and derives resolution from the provided physical distance.
}
\details{
The input \code{\link[terra:SpatVector-class]{SpatVector}} can be a line drawn along
the reference, or two points placed at the reference endpoints. CRS must be
\code{NULL} because coordinates are in pixel space.
}
\examples{
\dontrun{
scale_line <- terra::vect("scale.gpkg", layer = "scale")

calibration <- hsi_calibration_from_scale(
  scale_line,
  distance = 10,
  units = "mm"
)

scale_points <- terra::vect("points.gpkg", layer = "scale_points")

calibration <- hsi_calibration_from_scale(
  scale_points,
  distance = 10000,
  units = "um"
)
}

}
\seealso{
Other HSI Calibration:
\code{\link[=hsi_calibration_direct]{hsi_calibration_direct()}},
\code{\link[=hsi_calibration_from_dims]{hsi_calibration_from_dims()}},
\code{\link[=hsi_drop_crs]{hsi_drop_crs()}},
\code{\link[=hsi_pixels_to_units]{hsi_pixels_to_units()}},
\code{\link[=hsi_set_extent]{hsi_set_extent()}}
}
\concept{HSI Calibration}

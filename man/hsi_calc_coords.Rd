% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsi_calc_coords.R
\name{hsi_calc_coords}
\alias{hsi_calc_coords}
\title{Calculate physical coordinate rasters from pixel indices}
\usage{
hsi_calc_coords(x, um_per_pixel, filename = "", overwrite = FALSE, ...)
}
\arguments{
\item{x}{A \code{\link[terra:SpatRaster-class]{SpatRaster}} with hyperspectral data.}

\item{um_per_pixel}{Numeric. Physical size of one pixel in µm, as returned
by \code{hsi_calibration_direct()}, \code{hsi_calibration_from_dims()}, or
\code{hsi_calibration_from_scale()}.}

\item{filename}{Character. Output filename. Default \code{""} keeps result in memory.}

\item{overwrite}{Logical. Overwrite existing file. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[terra:writeRaster]{terra::writeRaster()}}.}
}
\value{
A \code{\link[terra:SpatRaster-class]{SpatRaster}} with two layers: \code{row_um}
(physical row position in µm) and \code{col_um} (physical column position in µm).
}
\description{
Calculate physical coordinate rasters from pixel indices
}
\details{
Pixel positions are expressed as pixel centres: the first pixel holds
\code{0.5 * um_per_pixel}, not zero. This is consistent with how
\code{\link[terra:extract]{terra::extract()}} addresses pixels and ensures alignment with physical
measurements taken at the pixel footprint centre.

Output is always in µm. Convert to mm by dividing by \code{1000}, to cm by
dividing by \code{10000}.
}
\examples{
\dontrun{
x <- terra::rast("REFLECTANCE_testdata.tif")
um <- hsi_calibration_from_dims(pixels = 50000, distance = 10000)
x_coords <- hsi_calc_coords(x, um_per_pixel = um)
x_coords <- hsi_calc_coords(
  x,
  um_per_pixel = um,
  filename = "coords.tif",
  overwrite = TRUE
)
}

}
\seealso{
\code{\link[=hsi_calibration_direct]{hsi_calibration_direct()}}, \code{\link[=hsi_calibration_from_dims]{hsi_calibration_from_dims()}},
\code{\link[=hsi_calibration_from_scale]{hsi_calibration_from_scale()}} to obtain \code{um_per_pixel}.
\code{\link[=hsi_shift_coords]{hsi_shift_coords()}} to anchor the coordinate raster to a known
reference position.

Other HSI Calibration: 
\code{\link{hsi_calibration_direct}()},
\code{\link{hsi_calibration_from_dims}()},
\code{\link{hsi_calibration_from_scale}()},
\code{\link{hsi_pixels_to_units}()},
\code{\link{hsi_set_physical_extent}()},
\code{\link{hsi_shift_coords}()}
}
\concept{HSI Calibration}
